name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 min
  workflow_dispatch:  # manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: curl -sf https://venn-movie-api.onrender.com/health
       